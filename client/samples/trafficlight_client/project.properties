wiseman.root=../..
dist.dir=dist
src.dir=src
lib.dir=${wiseman.root}/lib
gen.src.dir=gen-src
build.dir=build
run.dir=run
test.dir=test
doc.dist.dir=${dist.dir}/doc
build.classes.dir=${build.dir}/classes
test.build.dir=${test.dir}/build
test.build.classes.dir=${test.build.dir}/classes
test.src.dir=${test.dir}/src
test.lib.dir=${test.dir}/lib
test.run.dir=${test.dir}/run
jaxb.lib.dir=${lib.dir}
jaxrpc.lib.dir=${lib.dir}
client.jar.file=${wiseman.root}/traffic-client.jar
war.file=${dist.dir}/traffic.war
events.war.file=${dist.dir}/events.war
debug=true
