# Override any properties in ${user.home}/.ant.properties
#

ant.version=1.6.5
ant.library.dir=../tools/lib
junit.version=3.8.1
junit.library.dir=../se/test/lib
# catalina.home=/var/tomcat5

wiseman.root=../se
client.jar.name=wiseman-client.jar
proj.name=hpwsmanclient
version=iteration_4
samples.dir=samples
dist.dir=dist
etc.dir=etc
staging.dir
src.dir=src
lib.dir=lib
build.dir=build
run.dir=run
test.dir=test
doc.dir=docs
debug=true

gen.src.dir=test/gen-src
apidocs.dir=${doc.dir}/apidocs
doc.dist.dir=${dist.dir}/${doc.dir}
build.classes.dir=${build.dir}/classes

test.build.dir=${test.dir}/build
test.build.classes.dir=${test.build.dir}/classes
test.src.dir=${test.dir}/${src.dir}
test.lib.dir=${test.dir}/lib
test.run.dir=${test.dir}/${run.dir}

client.jar.file=${dist.dir}/${client.jar.name}
test.jar.file=${dist.dir}/client-test.jar
client.war.file.name=hpwsman.war
war.file.name=hpwsman.war
war.file=${dist.dir}/${war.file.name}
events.war.file=${dist.dir}/events.war
demo.webapp.folder=samples/demo-webapps
dist.name=${proj.name}_${version}
zip.base.dir=${build.dir}/zipbase
staging.dir=${zip.base.dir}/${proj.name}
